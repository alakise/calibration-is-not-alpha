# Publication status

This directory is a local academic companion candidate. It is **not currently
published**.

Two independent release gates remain unresolved:

1. The current TypeSafe customer agreement must be checked with TypeSafe before
   publishing benchmark or performance information about Jev.
2. GitHub CLI authentication for the configured account is currently invalid.

Until both gates are resolved, do not create a public repository, release, or
tag from this tree. The private source repository remains the system of record.

The offline tests and table/figure generation are safe to run locally and make
no paid inference or network calls.
